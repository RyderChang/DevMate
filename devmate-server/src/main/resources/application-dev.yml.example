# Copy only the settings needed for local development into application-dev.yml.
# This example intentionally contains no external-service credentials or connections.
server:
  port: ${SERVER_PORT:8080}

logging:
  level:
    com.devmate: DEBUG
