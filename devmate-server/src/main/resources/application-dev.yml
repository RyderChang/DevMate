server:
  port: ${SERVER_PORT:8080}

spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/devmate}
    username: ${DB_USERNAME:devmate}
    password: ${DB_PASSWORD}

logging:
  level:
    root: INFO
    com.devmate: DEBUG
