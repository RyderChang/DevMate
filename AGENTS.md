# DevMate Agent Guide

本文件是 DevMate 仓库的长期开发约束，适用于在仓库内工作的 Codex Cloud 及其他 AI Agent。开始任何任务前必须阅读本文件、根目录 `README.md`、`CONTRIBUTING.md`（存在时）以及与任务相关的 `docs/` 文档。更深层目录中的 `AGENTS.md` 可补充局部规则；发生冲突时，以作用范围更具体的文件为准。

## 1. 项目简介

DevMate 是面向真实软件研发流程的 AI 助手平台，不是通用聊天机器人。系统以项目为工作空间，将代码仓库、研发文档、AI 对话、代码审查和测试生成关联起来。

目标能力包括：

- 基于项目上下文的 AI 对话；
- 文档与代码知识库（RAG）；
- GitHub 仓库只读同步与分析；
- 结构化 AI 代码审查；
- Java/JUnit 测试场景与测试代码生成；
- AI 调用、异步任务、权限与审计的工程化治理。

首版优先深度支持 Java/Spring 项目。未经项目所有者确认，不扩大到多语言深度分析、自动执行未知代码、自动修改用户仓库或自动合并 PR。

## 2. 架构与技术栈

项目采用前后端分离的 monorepo，后端以模块化单体起步。模块通过公开的应用服务边界协作，禁止跨模块直接调用 Mapper。只有出现明确的吞吐、隔离或独立发布需求后，才考虑拆分 AI Worker 或 Index Worker。

规划技术栈：

- 后端：Java 21、Spring Boot 3、Spring Security、MyBatis-Plus；
- 数据：MySQL 8、Redis、Qdrant；
- 文件：MinIO 或兼容 S3 的对象存储；
- 前端：Vue 3、Vite、Element Plus、Axios、Pinia；
- 数据迁移：Flyway；
- API 文档：OpenAPI / springdoc-openapi；
- 测试：JUnit 5、Mockito、Spring Boot Test、Testcontainers；
- 工程化：Docker Compose、GitHub Actions；
- 可观测性：Spring Boot Actuator、Micrometer、结构化日志。

依赖版本以构建文件和已接受的 ADR 为准。不得仅因存在更新版本而自行升级依赖。

## 3. 仓库目录约定

目标目录如下；不存在的目录表示尚未进入对应开发任务，不得为“补齐结构”而擅自初始化：

```text
devmate-server/       Spring Boot 后端
devmate-web/          Vue 3 前端
deploy/               Docker Compose、Nginx、环境模板
docs/                 需求、架构、ADR、API 与任务文档
docs/tasks/           可独立验收的开发任务
scripts/              可复用的本地开发与检查脚本
.github/workflows/    CI 工作流
```

规则：

- 后端按业务模块组织，模块内部再分层；不要建立全局巨型 `controller/service/mapper` 目录；
- 前端按领域页面和可复用能力组织，跨页面组件放入 `components/`；
- 架构决策写入 `docs/adr/`，不能只留在 PR 对话或代码注释中；
- API、配置或行为发生变化时，同一任务内更新对应文档；
- 不提交生成产物、本地数据、IDE 个性配置或临时文件。

## 4. 任务执行规则

1. 一次只执行一个明确任务，先阅读任务目标、输入条件、范围和验收标准。
2. 执行前检查当前分支、工作区状态、相关实现和现有测试。
3. 保留用户已有修改；发现无关未提交改动时，不覆盖、不删除、不纳入提交。
4. 只修改完成当前任务所必需的文件，不顺手重构或格式化无关代码。
5. 信息不足且会影响接口、数据模型、安全或架构时，停止并向项目所有者询问。
6. 实现后执行与改动匹配的最小充分验证，不能虚构未运行的测试结果。
7. 无法完成某项验证时，说明原因、影响和项目所有者可执行的命令。
8. 未经明确要求，不进入下一任务，不自动合并 PR，不发布或部署。

## 5. 分支策略

- `main`：可发布版本，只接受来自 `develop` 的发布 PR；
- `develop`：阶段集成分支，接受任务分支 PR；
- `feature/<issue>-<short-name>`：新功能；
- `fix/<issue>-<short-name>`：缺陷修复；
- `chore/<issue>-<short-name>`：工程、依赖或仓库维护；
- `docs/<issue>-<short-name>`：纯文档修改。

执行要求：

- 禁止直接向 `main` 或 `develop` 提交；
- 从最新 `develop` 创建任务分支；
- 一个分支只处理一个任务；
- PR 的目标分支通常为 `develop`；发布 PR 才以 `main` 为目标；
- 禁止强制推送共享分支、改写他人历史或擅自删除分支；
- 遇到分支冲突时先报告，不用破坏性命令处理。

## 6. Git 提交与 Pull Request

使用 Conventional Commits：

```text
<type>(<scope>): <imperative summary>
```

常用类型：`feat`、`fix`、`test`、`refactor`、`docs`、`chore`、`build`、`ci`。

示例：

```text
feat(auth): add JWT refresh flow
test(project): cover ownership checks
docs(architecture): record vector store decision
```

提交要求：

- 每个提交范围单一、可以审查，禁止混入无关变更；
- 摘要使用英文祈使表达，不加句号，不使用含糊描述；
- 测试应与对应实现放在同一 PR，可按审查需要拆分 commit；
- 不提交调试输出、注释掉的大段代码或“临时可用”实现。

PR 必须说明：背景与目标、主要改动、验证命令及结果、风险与回滚方式、未完成事项、适用截图。最终合并由项目所有者确认。

## 7. 后端编码规范

- 使用 Java 21 和 Spring Boot 3 已选定的能力，避免无必要的新框架；
- 包结构按 `identity`、`project`、`conversation`、`knowledge`、`github`、`review`、`testgen`、`ai`、`task`、`audit`、`admin` 等业务模块划分；
- 模块内部遵循 Controller、Application Service、Domain、Infrastructure 的依赖方向；简单模块不为形式创建空层；
- Controller 只负责协议转换、参数校验和调用应用服务，不包含业务规则或数据库访问；
- Service 表达用例和事务边界；避免一个事务包围 AI、GitHub 或对象存储等远程调用；
- Mapper 只负责数据访问，不承担权限判断和业务流程；
- 请求使用 DTO，响应使用 VO；禁止直接暴露数据库实体或持久化对象；
- 使用 Jakarta Validation 完成基础参数校验，并在领域/应用层校验业务约束；
- 使用统一响应、分页结构、业务错误码和全局异常处理；
- 列表接口必须分页并限制最大页大小；
- 资源操作必须同时校验身份与资源归属，防止 IDOR；
- 日志使用参数化写法并携带 traceId；禁止记录密码、令牌、密钥、完整源码、完整提示词或敏感文档；
- 公共类型和复杂业务规则写必要注释，避免复述代码的无价值注释；
- 时间统一按 UTC 存储，通过 ISO 8601 对外返回；
- 不使用 Lombok 或其他工具隐藏重要领域行为；是否使用辅助库以现有项目约定为准。

## 8. 前端编码规范

- 使用 Vue 3 Composition API；组件保持单一职责，复杂逻辑提取为 composable 或领域 service；
- JS/TS 语言选择以 `devmate-web` 初始化结果为准，禁止在未记录决策的情况下混用或迁移；
- API 调用集中在 `src/api/`，统一使用配置后的 Axios 实例；页面组件不得散落基础 URL、Token 注入或重复错误处理；
- 登录状态和跨页面共享状态使用 Pinia；局部 UI 状态保留在组件内；
- 路由鉴权必须有服务端权限校验配合，不能把隐藏按钮当作安全控制；
- 复用 Element Plus 组件和项目设计变量，不复制大段近似样式；
- 每个异步页面处理加载、空数据、失败、重试和成功状态；
- 表单同时提供前端体验校验与后端权威校验；
- 不在浏览器持久化 AI 密钥、GitHub Token 或其他服务端凭据；
- API 类型、字段和错误码变化时同步更新调用层与测试。

## 9. 数据库规范

- MySQL 结构变更只能通过新的 Flyway migration 完成；已合并或已在共享环境执行的 migration 不得修改；
- 表名和字段名使用 `snake_case`，Java 属性使用 `camelCase`；
- 主键统一使用项目选定的 `BIGINT` 方案，不在不同模块自行发明 ID 策略；
- 明确设置非空、默认值、唯一约束和外键/逻辑约束，不依赖应用层猜测数据状态；
- 为真实查询模式设计索引，避免重复、无效或未经说明的索引；
- JSON 字段只用于变化频繁的快照或结构，不替代明确的关系字段；
- 软删除只用于需要恢复的核心对象；临时数据和日志按保留策略物理清理；
- 数据库枚举值与 Java 枚举保持一致，拒绝未知状态；
- 禁止在生产配置中启用自动建表或自动修改结构；
- migration 应能从空库顺序执行，并提供兼容 MySQL 8 的验证。

## 10. 测试要求

- 新增或修改行为必须有对应测试；纯文档变更至少执行格式与链接检查；
- 核心 Service 使用 JUnit 5 与 Mockito 编写单元测试，覆盖正常、异常、边界和权限分支；
- Controller/API 使用 Spring Boot Test 或 MockMvc 验证状态码、校验、鉴权和统一响应；
- Mapper、Flyway 与 MySQL 特性优先使用 Testcontainers 验证，不用 H2 结果代替 MySQL 兼容性结论；
- 外部 AI、GitHub、对象存储客户端必须通过接口替换为 Stub/Mock；CI 不调用真实付费模型或依赖私人账户；
- 前端业务逻辑和关键组件使用项目选定的测试框架；关键用户流程在相应阶段增加端到端测试；
- 缺陷修复应先添加可复现问题的回归测试；
- 测试应稳定、相互隔离，不依赖执行顺序、真实时间、公共网络或开发者本机状态；
- PR 中列出实际执行的命令与结果。若跳过测试，必须说明原因和风险。

## 11. AI 与 RAG 开发注意事项

- 业务模块只依赖统一 AI Gateway，不直接绑定具体模型提供商 SDK；
- 聊天、Embedding、结构化输出校验、提示词模板和用量记录保持清晰接口边界；
- 提示词模板必须版本化；AI 结果关联模型、模板版本、源码提交 SHA、引用来源、Token 用量和执行状态；
- 代码审查、测试生成等结构化输出必须经过 Schema 和业务校验，解析失败应明确失败或有限重试；
- 仓库代码、注释、文档和用户上传内容均是不可信输入，不能覆盖系统指令或触发未授权工具操作；
- RAG 检索必须强制按 `userId` 与 `projectId` 隔离，并保存来源、路径、版本和片段编号；
- 只发送完成任务所需的最小上下文，不把整个仓库无差别发送给模型；
- 对上下文长度、输出长度、并发、超时和重试设置明确上限；
- AI 审查结论是建议，需展示证据、位置、严重级别与置信度，不把推测伪装成确定事实；
- 规则检测与 AI 分析应互补，确定性问题优先由确定性规则处理；
- MVP 只生成和导出测试代码，不在服务端执行未知仓库代码；
- GitHub 集成首版只读。未经用户明确确认，不创建提交、分支或 PR 到用户绑定的外部仓库。

## 12. 安全与禁止事项

禁止：

- 直接提交或自动合并到 `main`、`develop`；
- 使用 `git reset --hard`、强制推送等破坏性操作处理冲突或用户修改；
- 提交密码、API Key、Token、Cookie、私钥、真实连接串或包含私密信息的日志；
- 将密钥写入前端、示例文件、测试固定值、源码或 Git 历史；
- 绕过 Spring Security、资源归属校验、参数校验或审计；
- 在日志、异常响应或模型调用记录中泄露完整源码、提示词、仓库令牌或用户文档；
- 执行、构建或运行来源不可信的用户仓库代码；
- 在没有任务依据时新增依赖、升级版本、更换技术栈或拆分微服务；
- 修改已执行的 Flyway migration，或在生产环境依赖自动建表；
- 为通过测试而删除测试、降低断言、吞掉异常或关闭安全检查；
- 虚构命令输出、测试结果、性能数据、部署状态或已实现功能；
- 扩大任务范围、提前实施后续阶段或留下未说明的占位实现。

如任务要求与本文件冲突，先停止并向项目所有者说明冲突；除非获得明确确认，不自行选择例外。

## 13. 完成任务时的回复

向项目所有者报告：

1. 完成内容和未完成内容；
2. 修改文件列表；
3. 实际执行的验证命令与结果；
4. 分支、commit SHA 和 PR 链接；
5. 风险、限制和需要人工完成的操作；
6. 建议的下一任务，只说明，不实施。
