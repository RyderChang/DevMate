# DevMate — Codex Cloud 执行任务 001

## 仓库治理与文档基线

> 任务类型：Repository Governance / Documentation
>
> 所属阶段：第一阶段——工程基座与项目空间
>
> 前置状态：仓库已创建，Codex Cloud 已连接；当前仅有 `README.md` 与 `.gitignore`
>
> 本任务禁止：编写业务代码、初始化 Spring Boot/Vue 工程、创建数据库脚本、接入 AI

---

## 一、可直接复制给 Codex Cloud 的任务说明

以下内容整体复制给 Codex Cloud 执行：

```text
你正在 DevMate GitHub 仓库中执行第一个工程任务：仓库治理与文档基线。

DevMate 是一个面向软件研发流程的 AI 助手平台，采用前后端分离的 monorepo。规划技术栈为 Java 21、Spring Boot 3、Spring Security、MyBatis-Plus、MySQL 8、Redis、Vue 3、Vite、Element Plus、Qdrant 和 MinIO。项目采用模块化单体起步，后续按需要拆分后台 Worker。

本任务只建立仓库规范、文档结构与空的工程边界。不要编写任何业务代码，不要初始化 Spring Boot 或 Vue 工程，不要添加 pom.xml、package.json、数据库迁移、Docker Compose、CI workflow 或 AI/GitHub 集成代码。

一、执行前检查

1. 先阅读仓库现有 README.md、.gitignore，以及仓库中可能存在的 AGENTS.md 或其他说明。
2. 使用 git status、git branch、git log 检查当前分支和工作区状态。
3. 保留用户已有内容。不要覆盖有价值的 README 或 .gitignore 内容；应在其基础上整理和补充。
4. 如果工作区存在与本任务无关的未提交修改，停止并报告，不要擅自覆盖、删除或提交。
5. 不得直接在 main 分支提交。

二、分支要求

目标分支模型：main ← develop ← feature 分支。

1. 检查远程是否已有 develop。
2. 如果 develop 已存在，从最新 develop 创建任务分支：
   chore/001-repository-governance
3. 如果 develop 不存在：
   - 以当前 main 为基线创建 develop 并推送；
   - 再从 develop 创建 chore/001-repository-governance；
   - 所有本任务文件修改只能发生在该任务分支。
4. 最终 Pull Request 的目标分支必须是 develop，不是 main。
5. 如果当前 Codex 环境无法创建或推送 develop，停止并清楚报告阻塞原因，不要改为向 main 提交或创建 PR。

三、需要修改的现有文件

1. README.md

将其整理为项目入口，但不要写成已经完成功能。至少包含：

- DevMate 名称与一句话定位；
- 当前状态明确标记为 Planning / Foundation；
- 核心能力规划：项目上下文对话、RAG 知识库、GitHub 项目分析、AI 代码审查、测试生成；
- 技术栈规划；
- monorepo 目录说明；
- 开发路线图，只标记“需求与架构基线”完成，其他功能不得标记完成；
- 文档入口，链接到 docs/README.md；
- 协作方式，链接到 CONTRIBUTING.md；
- 安全说明：不要提交密码、Token、API Key 或私有仓库内容；
- License 写为“暂未指定 / To be decided”，不要擅自选择开源许可证。

2. .gitignore

在保留原有有效规则的基础上，补充适用于以下环境的忽略规则：

- Java、Maven、Gradle 构建产物；
- IntelliJ IDEA；
- Node.js、Vue、Vite；
- VS Code 的本地个性化配置；
- 环境变量与本地密钥文件；
- 日志、临时文件、系统文件；
- Docker/MinIO/MySQL 等本地数据目录（只忽略明确的仓库内运行时目录，不使用过宽规则）；
- 测试覆盖率与前端构建产物。

必须保留可提交的示例环境文件，例如 `.env.example` 不得被忽略。不要使用可能误伤正式源码、文档或部署模板的宽泛通配规则。

四、需要创建的文件

1. AGENTS.md

创建仓库级 Codex 工作规范，至少写明：

- 先阅读 README、CONTRIBUTING 和相关 docs；
- 一次只执行一个明确任务；
- 不直接修改 main；从 develop 创建 feature/fix/chore/docs 分支；
- 不扩大任务范围，不顺手重构无关内容；
- 保护用户已有修改；
- 禁止提交密钥、Token、真实连接串和私有代码数据；
- 后续代码应遵循 DTO/VO、分层、参数校验、统一异常、日志和测试规范；
- 修改后执行与改动匹配的最小充分验证；
- PR 必须说明改动、验证、风险和未完成项；
- AI 生成内容和代码必须接受人工审核；
- 未经明确授权，不执行未知仓库代码，不写入用户绑定仓库，不自动合并 PR。

2. CONTRIBUTING.md

至少包含：

- main、develop、feature/fix/chore/docs 分支职责；
- 分支命名格式；
- Conventional Commits 规范与示例；
- 单任务开发流程；
- Pull Request 要求；
- 文档、代码、测试和安全检查清单；
- “最终合并由项目所有者确认”的规则。

3. .editorconfig

设置通用格式基线：

- UTF-8；
- LF；
- 文件末尾换行；
- 删除行尾空格（Markdown 可按需要例外）；
- Java 使用 4 空格；
- JavaScript、TypeScript、Vue、JSON、YAML 使用 2 空格；
- 不要加入与尚未创建的格式化工具冲突的复杂规则。

4. .gitattributes

统一文本文件使用 LF，并合理标记常见二进制文件。不要错误地把 Markdown、源码、SQL 或 YAML 标成 binary。

5. .github/pull_request_template.md

模板至少包含：

- 背景/目标；
- 改动内容；
- 验证方式与结果；
- 截图（如适用）；
- 风险与回滚；
- 未完成事项；
- 自检清单：范围、测试、文档、密钥、目标分支。

6. .github/ISSUE_TEMPLATE/feature.yml

创建功能任务表单，收集：背景、目标、范围、非范围、输入条件、实现建议、验收标准和补充信息。

7. .github/ISSUE_TEMPLATE/bug.yml

创建缺陷表单，收集：问题描述、复现步骤、预期行为、实际行为、环境、日志/截图和影响范围。提示用户删除敏感数据。

8. .github/ISSUE_TEMPLATE/config.yml

关闭空白 Issue；不要填写不存在的外部支持链接。

9. docs/README.md

作为文档导航，链接到项目基线、架构决策、后续 API 文档和开发文档的位置。不存在的未来文档应标记为“计划中”，不要创建虚假链接。

10. docs/requirements/devmate-baseline.md

创建项目需求与架构摘要，内容应与以下已确认基线一致：

- 产品不是通用聊天机器人，而是围绕软件项目上下文的研发助手；
- MVP 核心能力包括 AI 对话、RAG、GitHub 只读分析、代码审查和测试生成；
- 首先深度支持 Java/Spring，其他语言初期只做基础文本检索；
- 采用前后端分离 monorepo 与模块化单体；
- MySQL 保存业务数据，Redis 用于缓存/限流/任务协调，Qdrant 保存向量，MinIO 保存文件；
- GitHub 首版只读；不自动修改、执行或合并用户代码；
- 第一阶段只建设工程基座、认证和项目空间，暂不接入 AI；
- 用户角色为游客、普通用户、管理员；MVP 项目默认仅创建者可访问；
- 核心非功能要求：资源归属校验、密钥保护、提示词注入防护、异步任务幂等、结构化 AI 输出校验、用量记录和可测试性；
- 七阶段路线图：工程基座、AI 对话、RAG、GitHub 分析、代码审查、测试生成、管理与部署。

该文件只做可靠摘要，不应杜撰详细接口、表字段或已经实现的功能。

11. docs/architecture/README.md

说明架构文档目录用途，并记录当前架构概览：Vue 前端、Spring Boot API、业务模块、AI Gateway、异步任务、MySQL/Redis/Qdrant/MinIO、GitHub 与 AI 外部服务。可以使用一个简洁 Mermaid 图。明确这是目标架构而非当前已部署架构。

12. docs/adr/README.md

说明 ADR 的用途、状态和编号规则，并建立 ADR 索引。

13. docs/adr/0000-template.md

ADR 模板至少包含：标题、状态、日期、背景、决策、备选方案、后果、后续行动。

14. docs/adr/0001-use-modular-monolith.md

记录“首版采用模块化单体”的决策：

- 状态：Accepted；
- 背景：个人作品集、功能多、需要清晰模块边界但应控制运维复杂度；
- 决策：前后端分离，后端采用模块化单体，异步任务先与主应用同库/同代码库；
- 约束：模块通过应用服务边界协作，不跨模块直接调用 Mapper；
- 后果：部署简单、易于端到端测试，但必须主动维护边界；
- 演进条件：任务吞吐、资源隔离或发布节奏出现真实需求时，再优先拆分 AI Worker/Index Worker。

15. devmate-server/README.md

只说明未来后端目录的职责、计划技术栈和“尚未初始化”。不要创建 Java 文件或构建文件。

16. devmate-web/README.md

只说明未来前端目录的职责、计划技术栈和“尚未初始化”。不要创建 Vue 文件或构建文件。

17. deploy/README.md

只说明未来部署目录将保存 Docker Compose、Nginx 和环境模板，目前尚未实现。不要创建 Compose 配置。

18. scripts/README.md

只说明未来脚本目录用途及安全原则，目前不要创建执行脚本。

五、明确禁止创建或修改的内容

- 不创建任何 Java、JavaScript、TypeScript、Vue、SQL 业务或工程源文件；
- 不创建 pom.xml、build.gradle、package.json 或锁文件；
- 不创建 Docker Compose、Nginx 配置或数据库迁移；
- 不创建 GitHub Actions workflow，本项属于第一阶段后续任务；
- 不添加依赖，不运行项目脚手架；
- 不接入 OpenAI、GitHub OAuth、数据库、Redis、Qdrant 或 MinIO；
- 不添加真实密钥、示例假密钥或可被误认为真实凭据的字符串；
- 不创建 LICENSE，许可证由项目所有者后续决定；
- 不修改 GitHub 仓库可见性，不自动合并 PR，不删除原有分支。

六、内容质量要求

1. 所有文档使用简体中文；技术名词和 commit 信息可使用英文。
2. 文档必须区分“当前状态”和“规划状态”，不能把未实现能力描述成已经可用。
3. Markdown 标题层级、列表、表格和相对链接应正确。
4. 同一规则只设一个权威来源：README 做入口，CONTRIBUTING 管协作，AGENTS.md 管 Codex 行为，docs 存设计信息；避免复制大量重复文字。
5. 不虚构测试结果、部署状态、徽章、演示地址或贡献者信息。

七、验证要求

完成编辑后至少执行并报告：

- git status --short
- git diff --check
- git diff --stat
- 检查所有新增 Markdown 相对链接指向已存在文件，或明确标记为计划中且不创建链接；
- 检查 YAML 基本语法和 GitHub Issue Form 必填字段；
- 检查仓库中没有被意外加入密钥、环境文件、构建产物和业务代码；
- 检查原 README.md 与 .gitignore 中有价值的内容已被保留或合理整合。

如果没有可用的 Markdown/YAML 检查工具，不要为了本任务临时引入依赖；进行人工结构检查并在报告中说明。

八、提交与 Pull Request

在所有验证通过后创建一个提交：

chore(repo): establish governance and documentation baseline

提交只包含本任务文件。推送 chore/001-repository-governance，并创建目标为 develop 的 Pull Request。

建议 PR 标题：

chore(repo): establish governance and documentation baseline

PR 描述必须列出：

- 修改/新增文件；
- 验证命令和结果；
- 未编写业务代码；
- 未创建 CI、后端或前端工程；
- GitHub 分支保护仍需项目所有者在仓库 Settings 中确认（如 Codex 无管理权限）；
- LICENSE 尚未选择；
- 后续任务是“后端工程初始化”，但本 PR 不实施它。

九、最终回复格式

任务结束时请按以下结构回复：

1. 完成摘要
2. 分支与提交 SHA
3. 修改文件列表
4. 验证结果
5. Pull Request 链接及目标分支
6. 需要项目所有者手动完成的 GitHub 设置
7. 已知限制与下一步（只说明，不实施）

不要自动合并 Pull Request。完成后等待项目所有者审核。
```

---

## 二、项目负责人核对结果

### 当前阶段判断

| 检查项 | 当前状态 | 判断 |
| --- | --- | --- |
| GitHub 仓库 | 已创建 | 满足启动条件 |
| Codex Cloud | 已连接仓库 | 满足远程执行条件 |
| 需求与架构 | 已确定 | 可作为任务基线 |
| 仓库内容 | 仅 README 与 .gitignore | 尚未提前进入编码，符合计划 |
| 业务代码 | 尚未创建 | 符合“先治理、后初始化工程”的顺序 |
| 分支体系 | 当前只有 main | 任务 001 必须建立 develop 与任务分支 |

结论：项目当前处于“阶段 0 完成、第一阶段任务 001 待执行”。没有发现与计划冲突的进度。该判断基于项目所有者提供的仓库状态；Codex 执行时仍需通过只读 Git 命令复核实际仓库。

### 为什么第一个任务不初始化工程

仓库规则和文档基线会约束后续所有任务。先建立分支、提交、PR、文档和 Codex 行为规范，能够避免后续一次性生成大量代码、绕过 `develop`、把密钥写入仓库，或者把规划功能错误地标记为已实现。

---

## 三、文件变更清单

### 修改

| 文件 | 修改目标 |
| --- | --- |
| `README.md` | 建立准确的项目入口、当前状态、路线图和文档导航 |
| `.gitignore` | 补全 Java/Vue/IDE/密钥/运行时文件忽略规则，并保留 `.env.example` |

### 创建

| 文件 | 用途 |
| --- | --- |
| `AGENTS.md` | 约束后续 Codex 在仓库中的工作方式 |
| `CONTRIBUTING.md` | 分支、commit、PR 与验收规范 |
| `.editorconfig` | 跨编辑器基础格式 |
| `.gitattributes` | 换行与文本/二进制属性 |
| `.github/pull_request_template.md` | PR 标准模板 |
| `.github/ISSUE_TEMPLATE/feature.yml` | 功能任务表单 |
| `.github/ISSUE_TEMPLATE/bug.yml` | 缺陷任务表单 |
| `.github/ISSUE_TEMPLATE/config.yml` | Issue 模板配置 |
| `docs/README.md` | 文档导航 |
| `docs/requirements/devmate-baseline.md` | 已确定需求与架构摘要 |
| `docs/architecture/README.md` | 目标架构概览 |
| `docs/adr/README.md` | ADR 索引与规则 |
| `docs/adr/0000-template.md` | ADR 模板 |
| `docs/adr/0001-use-modular-monolith.md` | 模块化单体决策记录 |
| `devmate-server/README.md` | 后端工程目录占位说明 |
| `devmate-web/README.md` | 前端工程目录占位说明 |
| `deploy/README.md` | 部署目录占位说明 |
| `scripts/README.md` | 脚本目录占位说明 |

本任务不创建 `LICENSE`、CI workflow、构建文件、源代码或部署配置。

---

## 四、验收标准

任务只有同时满足以下条件才可交付：

1. 所有修改位于 `chore/001-repository-governance`，没有直接提交到 `main`。
2. `develop` 已存在于远程，PR 的 base 明确为 `develop`。
3. README 准确区分当前状态与规划功能，没有虚假完成标记、徽章或地址。
4. 两个既有文件的有效内容得到保留或合理整合。
5. 文件清单完整，职责不重叠，Markdown 相对链接有效。
6. Issue Form YAML 结构有效，且没有虚构外部链接。
7. `.gitignore` 覆盖计划技术栈，但不会忽略 `.env.example`、源码或文档。
8. 仓库中没有新增业务代码、构建配置、CI、Docker 配置、数据库脚本或依赖锁文件。
9. 仓库中不存在密码、Token、API Key、真实连接串或私有项目内容。
10. `git diff --check` 通过，并提交验证结果。
11. 只有一个范围清晰的 commit，信息为：
    `chore(repo): establish governance and documentation baseline`
12. 已创建面向 `develop` 的 PR，但没有自动合并。
13. Codex 最终报告包含 commit SHA、PR 链接、验证结果、限制和需要手动完成的设置。

---

## 五、项目所有者在 PR 后的人工检查

Codex 完成任务后，项目所有者需要：

1. 确认 PR base 是 `develop`，不是 `main`；
2. 抽查 README 中所有“已完成/规划中”状态；
3. 检查 `.gitignore` 是否保留 `.env.example`；
4. 在 GitHub Settings 中为 `main` 和 `develop` 配置分支保护或 Ruleset；
5. 禁止直接 push，要求通过 PR；
6. 当前尚无 CI 时，不要勾选不存在的必需状态检查；
7. 审核后由项目所有者合并，不让 Codex 自动合并；
8. LICENSE 暂不选择，后续作为独立决策处理。

建议 `main` 只接收来自 `develop` 的发布 PR，`develop` 接收 feature/fix/chore/docs 分支的日常 PR。